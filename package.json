{
  "name": "wsa-fse-sync",
  "version": "1.0.0",
  "description": "FSEconomy data synchronization for Western Skies Air",
  "main": "fse-sync.js",
  "scripts": {
    "start": "node fse-sync.js",
    "dev": "nodemon fse-sync.js",
    "sync": "node -e \"require('./fse-sync.js').syncFSEData().then(console.log)\"",
    "test-firebase": "node test-firebase.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.0",
    "express": "^4.18.0",
    "firebase-admin": "^12.0.0",
    "resend": "^6.12.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
